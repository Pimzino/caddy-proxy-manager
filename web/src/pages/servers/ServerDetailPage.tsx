// One server: facts, live resource charts (useServerSamples), Caddy state/actions, sync state, traffic section. Owner: WEB-SERVERS builder. See SPEC.md "Round 3".
export default function ServerDetailPage() {
  return null;
}
